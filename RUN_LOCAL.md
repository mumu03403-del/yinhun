# 怎么玩到这个游戏（非技术用户照着做就行）

> 这一步不需要安装任何软件、不需要联网下载任何东西。

---

## 最简单的方式：双击打开（推荐先试这个）

1. 打开文件夹：
   `C:\Users\37615\Projects\game\build\h5-fallback\`
2. **双击 `index.html`**
3. 浏览器会打开游戏，直接就能玩。

已经在 Chrome 上实测过这条路径：能正常开局、能合并、金币会涨、存档也能存（见 `build\_evidence\file_protocol_check.json`）。

> 小提示：如果双击后是用「记事本」打开的，右键 `index.html` → 打开方式 → 选 Chrome 或 Edge。

---

## 更稳的方式：起一个本地服务再打开

如果双击那条路在你的电脑上不正常，用这个方式（多一步，但最稳）：

1. 打开「命令提示符」（按 `Win + R`，输入 `cmd`，回车）
2. 粘贴下面两行，每行回车：

```
cd /d C:\Users\37615\Projects\game\build
C:\Users\37615\.workbuddy\binaries\python\versions\3.13.12\python.exe _serve.py 8080 h5-fallback
```

3. 看到 `serving ... at http://127.0.0.1:8080/` 就说明起来了
4. 浏览器打开：**http://127.0.0.1:8080/index.html**

关掉服务：在那个黑窗口里按 `Ctrl + C`。

> 注意：用这个方式时，**必须**通过 `http://127.0.0.1:8080/index.html` 打开，
> 不要直接双击 `_serve.py` 所在目录里的 html 文件。

---

## 手机上玩

这个版本是按竖屏手机做的（375×812），在手机上体验最好。两种办法：

**办法 A：把文件夹拷到手机**
把整个 `h5-fallback` 文件夹拷进手机，用手机浏览器打开里面的 `index.html`。
（部分手机浏览器不允许本地文件读存档，游戏本身能玩，只是可能不记录进度。）

**办法 B：让手机和电脑连同一个 WiFi**
1. 按上面「更稳的方式」把服务起起来
2. 在电脑上运行 `ipconfig`，找到「IPv4 地址」，例如 `192.168.1.7`
3. 手机浏览器打开 `http://192.168.1.7:8080/index.html`

---

## 怎么玩

| 操作 | 效果 |
| --- | --- |
| **点一下**一个单位，再点另一个**同档位**的单位 | 两个合成一个，档位 +1（这是原型原本的玩法） |
| **按住一个单位拖到**另一个同档位单位上松手 | 同样合并（拖拽时，所有能合的目标会亮绿边） |
| **长按一个满级（6 档）单位 0.5 秒** | 结算成金币并腾出这一格（按住时会有一圈金色进度环） |
| 点右下角 **招募 +1** | 在随机空格放 1 个 1 档单位（免费） |
| 点右上角 **图鉴** | 看角色收集进度；合出对应档位自动解锁 |
| 点底部 **重置存档** | 清空进度重新开始（会二次确认） |

**规则**
- 网格 5 列 × 4 行 = 20 格，最高 **6 档**。两个满级单位不能再合并。
- 每个单位按档位**每秒自动产金币**：1 档 +1/秒、2 档 +3、3 档 +9、4 档 +27、5 档 +81、6 档 +243。
- **满级单位可以长按结算**：长按 0.5 秒 → 兑换成 **该档 30 秒的产出**（6 档 = +7290 金币）→ 该格空出来。
  这样即使 20 格全被满级单位占满，也能靠结算腾出空位继续玩，不会卡死。
- 进度**自动存档**（每 2 秒一次，切后台/关页面也会存）。
- 关掉再打开会结算**离线收益**：按离线时长 × 每秒产出 × **50%**，最多算 **8 小时**。
- 图鉴共 6 张卡：**合出第几档就解锁第几张**（1 档解锁第 1 张，合出 6 档即集满 6 张）。

---

## 关于画面

现在全部是**代码画的纯色块**（灰/蓝/绿/黄/橙/红 = 1~6 档），没有任何图片素材，
也不含任何已有动漫角色的形象。这是刻意的占位美术，正式美术还没开始做。

---

## 找不到文件 / 打不开怎么办

| 现象 | 处理 |
| --- | --- |
| 双击是空白页 | 换成 Chrome / Edge 打开；或改用「更稳的方式」起服务 |
| 页面右下角显示「尚无存档（首次开局）」 | 正常，说明是第一次玩 |
| 手机上进度不保存 | 手机浏览器对本地文件的限制，改用「办法 B」 |
| 想彻底重来 | 点游戏底部「重置存档」，或在浏览器里按 `Ctrl+Shift+Delete` 清缓存 |
