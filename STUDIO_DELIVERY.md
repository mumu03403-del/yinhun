# 银魂主题·合并+放置 微信小游戏 — 工作室汇编交付（极速原型）

> 主理人：游承峰（Game Development Studio）
> 日期：2026-09-19
> 阶段：Phase 0 诊断 → 概念孵化（极速原型强度，流程文档后置）

## 一、已确认的方向决策
| 项 | 决策 |
|---|---|
| 品类 | 合并（Merge）+ 放置（Idle）+ 角色图鉴收集 |
| 平台 | 微信小程序游戏 |
| 核心循环 | 合并升级解锁角色卡 → 图鉴挂机产金 → 金币加速生成/扩格 |
| 美术方向 | 动漫还原（忠实日式动画观感）|
| 引擎（默认）| Cocos Creator 3.x（未最终拍板，可切换）|
| 推进强度 | 极速原型（先出可玩 demo，GDD/架构/QA 后置）|

## 二、交付物清单（均已读盘验证）
| 文件 | 负责人 | 内容 |
|---|---|---|
| `design/concept/gintama-merge-concept.md` | 设计策略 | 核心循环 / 图鉴 / 数值基线（占位）/ IP 阻断项 |
| `art/art-direction.md` | 美术总监 | 动漫还原调性 / 占位策略 / 卡牌 UI 规格 / 配色字体 |
| `README.md` | 工程负责人 | 运行预览步骤 / 已实现 vs 未实现 / 引擎切换成本 |
| `assets/src/...` + `settings/` + `project.json` 等 23 个文件 | 工程负责人 | Cocos Creator 3.x 可运行原型脚手架（合并网格+放置收益+图鉴入口，100% 占位美术）|

## 三、跨成员一致性核对（主理人发现）
设计稿与工程实现存在**范围裁剪差异**，不影响跑通，但需你定夺是否对齐：

| 维度 | 设计稿（目标）| 工程实际（原型）| 建议 |
|---|---|---|---|
| 棋盘 | 5×5 = 25 格 | 5×4 = 20 格 | 原型够用；扩到 25 格改 `GameConfig.gridRows=5` |
| 最高阶 | 8 阶 | 6 阶 | 原型够用；改 `maxTier` + 补 `tierColors/coinRate` |
| 角色卡 | 3 线 ×8 阶 = 24 张 | 6 张 | 原型够用；补 `characterNames` 数组 |
| 挂机公式 | `1 × 1.8^(tier-1)` | `coinRate=[1,3,9,27,81,243]`（≈`3^(tier-1)`）| 需统一；建议以工程值为准或重设 |
| 合并奖励 | `10 × 2^(N-1)` | 未实现一次性合并奖励（仅挂机产金）| 原型可后补 |

**结论**：工程方做了合理原型裁剪。建议把设计稿明确拆成「目标 v1」与「原型当前」两栏，避免混淆；或让工程直接对齐设计稿（改 `GameConfig` 即可，零架构改动）。

## 四、已知风险与缓解
| 风险 | 等级 | 缓解 |
|---|---|---|
| **IP 版权**：银魂商用侵权，构成上线硬阻断 | 🚫 BLOCKER | 上线前二选一：① 拿正规授权；② 完全去 IP 化改原创角色（推荐），保留玩法与动漫还原调性。原型所有命名/美术已占位去 IP |
| 占位角色名仍带 Gintama 味（"天然卷剑客/甜党使者/攘夷志士"）| 中 | 真上线前改为完全原创名（如"阿八/小乐"），避免概念擦边 |
| 沙箱无法运行 Cocos（无 GUI/CLI 构建）| 低（流程）| 需你本地用 Cocos Creator 3.8 + 微信开发者工具预览；README 已给步骤 |
| 挂机数值易通胀 | 中 | 离线封顶 + 软通涨控制，设计稿已给 8h/50% 占位 |
| 合并品类竞争激烈 | 中 | 差异化走"图鉴收集向"，设计稿已锚定 |

## 五、剩余 TODO 与卡点（汇报铁律）
**已完成**
- 概念孵化三件套（设计/美术/工程脚手架）已交付并落盘验证。

**进行中 / 待你决策**
- [ ] 设计稿 ↔ 工程数值对齐（见第三节，需你拍板"对齐设计"还是"设计拆目标/当前两栏"）
- [ ] 引擎最终拍板：Cocos 3.x（默认） / LayaAir / 原生 —— 切换成本见 README
- [ ] IP 方向决策：授权 or 原创去 IP 化（上线前必解）

**未做（极速原型刻意后置，非遗漏）**
- [ ] 真机/编辑器预览验证（沙箱无 Cocos，owner=你本地执行）
- [ ] 拖拽合并、动画/音效、存档与离线收益、真实美术与角色
- [ ] 完整 GDD / 架构评审 / QA 门控

**卡点（Blocker）**
1. **IP 版权** —— 唯一上线硬阻断，方向需你拍板（不影响当前原型）。
2. **本地预览** —— 沙箱环境限制，需你本地打开 Cocos 验证（非阻塞，仅验证路径）。

## 六、给你的下一步建议
1. 先本地按 README 跑通原型，确认"合并+挂机+图鉴"手感（约 10 分钟）。
2. 回复我：① 设计/工程是否对齐；② 引擎定哪个；③ IP 走授权还是原创。
3. 三项定后，我进入 Phase 2（系统设计）补完整 GDD + 真实美术管线，或 Phase 4 做垂直切片。
